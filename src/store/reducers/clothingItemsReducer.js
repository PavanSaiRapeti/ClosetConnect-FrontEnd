const initialState = {
  };

const clothingItemsReducer = (state = initialState, action) => {
    switch (action.type) {
     
      default:
        return state;
    }
  };

export default clothingItemsReducer;